#!/usr/bin/env bash

if [ -r "$1" ]; then
    SCRIPT_PATH=$(cygpath -w "$1")
    shift
    exec env NODE_PATH="$(npm list -g 2>/dev/null | head -n 1)\node_modules" "$(cygpath \"$(type -P node)\")" "$SCRIPT_PATH" "$@"
fi
