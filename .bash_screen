if [ $TERM == "screen" ]; then
    echo "WARNING: Screen got invoked into screen, ignoring"
    return
fi

if ! ps -eo args | grep -q '^screen'; then
    screen -d -m
fi
screen -A -x

# screen -ls
# screen -r
# DOC: http://www.bangmoney.org/presentations/screen.html