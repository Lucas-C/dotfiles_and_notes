#!/usr/bin/lua
        function noitacifiniuq(gabarit)    while true do print("#!/usr/bin/lua\n", gabarit, "\[\[", gabarit, "\]\])") end end    noitacifiniuq(        [[        function noitacifiniuq(gabarit)    while true do print("#!/usr/bin/lua\n", gabarit, "\[\[", gabarit, "\]\])") end end    noitacifiniuq(        ]])
